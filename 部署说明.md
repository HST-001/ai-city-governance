# 街道更新维护治理系统部署说明

本文档提供了两种部署方式的详细说明：传统部署方式和Docker容器化部署方式。您可以根据自己的需求和技术熟悉度选择合适的方式。

## 目录

- [传统部署方式](#传统部署方式)
- [Docker部署方式](#docker部署方式)
- [两种部署方式的切换](#两种部署方式的切换)
- [常见问题](#常见问题)

## 传统部署方式

### 环境要求

- Java JDK 17+
- PostgreSQL 15+
- Redis 7+
- Maven 3.8+
- Node.js 18+
- npm 或 yarn

### 后端部署

1. **准备数据库**
   - 创建PostgreSQL数据库：`urban_management`
   - 创建用户：`admin`，密码：`password`
   - 赋予用户数据库所有权限

2. **配置Redis**
   - 启动Redis服务
   - 默认端口：6379

3. **配置后端应用**
   - 编辑 `backend/src/main/resources/application.properties` 文件
   - 根据需要调整数据库连接信息、端口等配置

4. **编译和运行后端**
   ```bash
   cd backend
   mvn clean package
   java -jar target/urban-management-1.0.0.jar
   ```

### 前端部署

1. **安装依赖**
   ```bash
   cd frontend
   npm install
   ```

2. **开发环境运行**
   ```bash
   npm run dev
   ```

3. **生产环境构建**
   ```bash
   npm run build
   ```

4. **部署生产版本**
   - 将 `dist` 目录中的文件部署到Nginx或其他Web服务器
   - 配置Web服务器的代理，将 `/api` 请求转发到后端服务

## Docker部署方式

### 环境要求

- Docker 20.10+
- Docker Compose 2.0+

### 部署步骤

1. **准备工作**
   - 确保Docker已安装并运行
   - 复制示例环境配置文件（可选）
   ```bash
   cp .env.example .env
   ```

2. **启动所有服务**
   ```bash
   docker compose up -d
   ```
   此命令会自动构建镜像并启动所有服务：
   - 前端服务：http://localhost
   - 后端服务：http://localhost:8080/api
   - PostgreSQL：localhost:5432
   - Redis：localhost:6379

3. **查看服务状态**
   ```bash
   docker compose ps
   ```

4. **查看日志**
   ```bash
   # 查看所有服务日志
   docker compose logs
   
   # 查看特定服务日志
   docker compose logs backend
   ```

5. **停止服务**
   ```bash
   docker compose down
   ```

### 自定义配置

如果需要自定义配置，可以通过以下方式：

1. **修改 docker-compose.yml**
   - 调整端口映射
   - 更改环境变量
   - 添加或修改挂载卷

2. **使用环境变量文件**
   - 创建 `.env` 文件（基于 `.env.example`）
   - 在文件中设置所需的环境变量

## 两种部署方式的切换

### 从传统方式切换到Docker方式

1. **备份数据**
   - 备份PostgreSQL数据库
   ```bash
   pg_dump -U admin -d urban_management > backup.sql
   ```
   - 备份上传的文件（`./uploads` 目录）

2. **启动Docker服务**
   ```bash
   docker compose up -d
   ```

3. **恢复数据**（如果需要）
   - 将备份的数据库导入Docker中的PostgreSQL
   ```bash
   docker exec -i postgres_db psql -U admin -d urban_management < backup.sql
   ```
   - 将上传的文件复制到 `backend/uploads` 目录

### 从Docker方式切换回传统方式

1. **备份数据**
   - 导出Docker中的PostgreSQL数据
   ```bash
   docker exec -i postgres_db pg_dump -U admin -d urban_management > backup.sql
   ```
   - 备份上传的文件（`backend/uploads` 目录）

2. **停止Docker服务**
   ```bash
   docker compose down
   ```

3. **按照传统部署方式的步骤配置环境并部署**

4. **恢复数据**
   - 将备份的数据库导入PostgreSQL
   ```bash
   psql -U admin -d urban_management < backup.sql
   ```
   - 将上传的文件复制到相应目录

## 常见问题

### 1. Docker服务无法启动

- **检查Docker是否正在运行**
  - Windows: 打开Docker Desktop
  - 检查系统托盘图标状态

- **查看具体错误信息**
  ```bash
  docker compose logs --tail=50
  ```

- **端口冲突**
  - 检查是否有其他服务占用了80、5432或6379端口
  - 修改 `docker-compose.yml` 中的端口映射

### 2. 数据库连接失败

- **检查数据库容器状态**
  ```bash
  docker compose ps db
  ```

- **确保数据库正确初始化**
  ```bash
  docker compose logs db
  ```

- **验证连接参数**
  - 检查环境变量配置
  - 确保用户名和密码正确

### 3. 前端无法连接后端

- **检查后端服务状态**
  ```bash
  docker compose ps backend
  ```

- **确认API地址配置正确**
  - 在浏览器中尝试直接访问API：http://localhost:8080/api/actuator/health

- **检查CORS设置**
  - 确保后端CORS配置正确

### 4. 文件上传问题

- **检查文件权限**
  - 确保Docker容器对上传目录有写入权限

- **检查文件大小限制**
  - 查看后端配置中的文件上传大小限制
  ```properties
  spring.servlet.multipart.max-file-size=50MB
  spring.servlet.multipart.max-request-size=100MB
  ```

## 总结

- **传统部署方式**：适合对Java、Node.js等技术栈熟悉的用户，调试和修改更直接
- **Docker部署方式**：适合希望简化环境配置、快速部署的用户，保证环境一致性

两种部署方式可以根据需要灵活切换，本项目保留了对两种方式的完全支持，用户可以根据自己的技术熟悉程度选择合适的部署方式。