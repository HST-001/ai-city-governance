# AI城市治理系统 - 云服务器部署指南

## 目录
1. [方案选择](#方案选择)
2. [云服务器购买](#云服务器购买)
3. [服务器配置](#服务器配置)
4. [项目部署](#项目部署)
5. [域名配置](#域名配置)
6. [SSL证书配置](#ssl证书配置)
7. [运维管理](#运维管理)

---

## 方案选择

### 推荐方案：阿里云ECS

**优势：**
- 国内访问速度快
- 稳定可靠
- 价格合理
- 技术支持好

**配置建议：**
- CPU: 2核
- 内存: 4GB
- 存储: 40GB SSD
- 带宽: 5Mbps
- 操作系统: Ubuntu 20.04 LTS

**预估费用：**
- 按量付费: 约 0.5-1元/小时
- 包年包月: 约 200-400元/月

---

## 云服务器购买

### 阿里云购买步骤

1. 访问阿里云官网: https://www.aliyun.com
2. 注册/登录账号
3. 进入ECS控制台
4. 点击"创建实例"
5. 配置参数：
   - 地域: 选择离HR最近的区域
   - 实例规格: 2核4GB
   - 镜像: Ubuntu 20.04 64位
   - 存储: 40GB SSD
   - 带宽: 5Mbps
6. 设置密码（请记住！）
7. 确认订单并支付

### 安全组配置

1. 进入ECS控制台
2. 选择实例 -> 安全组
3. 配置入站规则：
   - 端口: 22, 协议: TCP, 授权对象: 0.0.0.0/0（SSH访问）
   - 端口: 80, 协议: TCP, 授权对象: 0.0.0.0/0（HTTP访问）
   - 端口: 443, 协议: TCP, 授权对象: 0.0.0.0/0（HTTPS访问）

---

## 服务器配置

### 连接服务器

#### Windows用户
使用PowerShell或CMD：
```bash
ssh root@服务器公网IP
```

#### Mac/Linux用户
```bash
ssh root@服务器公网IP
```

### 运行自动配置脚本

1. 上传部署脚本到服务器：
```bash
scp deploy/server-setup.sh root@服务器公网IP:/root/
```

2. 连接到服务器：
```bash
ssh root@服务器公网IP
```

3. 运行配置脚本：
```bash
chmod +x /root/server-setup.sh
/root/server-setup.sh
```

---

## 项目部署

### 方式1：使用SCP上传（适合小项目）

1. 在本地打包项目：
```bash
cd "D:\AI Coding Learning\AI+城市治理"
tar -czf ai-city-governance.tar.gz --exclude='node_modules' --exclude='.git' .
```

2. 上传到服务器：
```bash
scp ai-city-governance.tar.gz root@服务器公网IP:/opt/
```

3. 在服务器上解压：
```bash
ssh root@服务器公网IP
cd /opt
tar -xzf ai-city-governance.tar.gz
cd ai-city-governance
```

### 方式2：使用Git（推荐）

1. 将项目推送到GitHub/GitLab
2. 在服务器上克隆：
```bash
cd /opt
git clone https://github.com/your-username/ai-city-governance.git
cd ai-city-governance
```

### 启动服务

```bash
cd /opt/ai-city-governance
docker-compose up -d
```

### 查看服务状态

```bash
docker ps
```

### 查看日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f frontend
docker-compose logs -f backend
docker-compose logs -f postgres
```

---

## 域名配置（可选但推荐）

### 购买域名

推荐平台：
- 阿里云
- 腾讯云
- GoDaddy

### DNS解析

1. 进入域名控制台
2. 添加A记录：
   - 主机记录: @
   - 记录值: 服务器公网IP
   - TTL: 600

---

## SSL证书配置（推荐）

### 使用Let's Encrypt免费证书

1. 安装Certbot：
```bash
apt install certbot python3-certbot-nginx -y
```

2. 获取证书：
```bash
certbot --nginx -d yourdomain.com
```

3. 自动续期：
```bash
certbot renew --dry-run
```

### 修改Nginx配置

需要修改 `frontend/nginx.conf` 以支持HTTPS：

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;

    # 其他配置...
}
```

---

## 运维管理

### 日常维护

#### 查看服务状态
```bash
docker ps
```

#### 重启服务
```bash
docker-compose restart
```

#### 更新代码
```bash
cd /opt/ai-city-governance
git pull
docker-compose up -d --build
```

#### 备份数据库
```bash
docker exec ai-city-governance-db pg_dump -U postgres urban_management > backup_$(date +%Y%m%d).sql
```

#### 恢复数据库
```bash
docker exec -i ai-city-governance-db psql -U postgres urban_management < backup_20250213.sql
```

### 监控

#### 查看服务器资源
```bash
top
htop  # 需要安装: apt install htop
```

#### 查看磁盘使用
```bash
df -h
```

#### 查看日志
```bash
docker-compose logs --tail=100
```

### 安全建议

1. **定期更新系统**
```bash
apt update && apt upgrade -y
```

2. **配置防火墙**
```bash
ufw status
ufw allow 端口号
```

3. **修改SSH端口**
编辑 `/etc/ssh/sshd_config`，修改Port为其他端口

4. **定期备份数据**
建议设置自动备份脚本

---

## 常见问题

### Q1: 无法访问网站
**A:** 检查以下几点：
1. 安全组是否开放80端口
2. 防火墙是否允许80端口
3. Docker容器是否正常运行
4. Nginx配置是否正确

### Q2: 数据库连接失败
**A:** 检查以下几点：
1. PostgreSQL容器是否运行
2. 数据库密码是否正确
3. 网络配置是否正确

### Q3: 上传文件失败
**A:** 检查以下几点：
1. uploads目录权限是否正确
2. 磁盘空间是否充足
3. 文件大小是否超过限制

### Q4: 系统运行缓慢
**A:** 检查以下几点：
1. 服务器资源是否充足
2. 网络带宽是否足够
3. 数据库查询是否优化

---

## 成本估算

### 阿里云ECS（按量付费）
- 2核4GB: 约 0.5-1元/小时
- 月费用: 约 360-720元

### 阿里云ECS（包年包月）
- 2核4GB: 约 200-400元/月
- 年费用: 约 2400-4800元

### 其他费用
- 域名: 约 50-100元/年
- SSL证书: 免费（Let's Encrypt）

---

## 联系支持

如有问题，请联系：
- 阿里云技术支持: https://www.aliyun.com/support
- Docker官方文档: https://docs.docker.com
- 项目GitHub Issues

---

**部署完成后，HR可以通过以下方式访问：**
- HTTP: http://服务器公网IP
- HTTPS: https://yourdomain.com（如果配置了域名和SSL）

**祝部署顺利！**